# 폼 프로젝트 반자동 구축 워크플로우 (Netlify + Supabase)

> **사용법**: 새 대화를 열 때 이 파일을 첨부하고, 프로젝트 목적만 간단히 말하거나
> "만들자"라고 하면, 클로드는 이 문서의 **작업 원칙**을 항상 지키면서
> **챕터 0부터 순서대로** 질문하고 진행합니다.
>
> 예시: "이 워크플로우로, oo업체 견적문의 폼 만들고 싶어" / "만들자"

---

## 🎯 작업 원칙 (전체 챕터에 항상 적용)

1. **성공 전까지 리디렉션 금지** — 쿼리문(DB insert 등)이 성공으로 확인되기 전까지는 페이지 이동시키지 않음. 그 전까지는 `alert`로 성공/실패만 알림
2. **디버깅 우선순위**:
   - 프론트 문제 → 개발자 도구(콘솔) + 폼 입력값 출력해서 확인
   - 백엔드 문제 → 단계별로 Netlify Function 로그, Supabase 로그 확인
   - → 그러므로 **중요 단계마다 값/상태를 콘솔에 찍는 코드가 필수**
3. **유연한 핸들링** — 사용자 작업 흐름에 맞춰 챕터/스텝 순서를 유연하게 조정
4. **모르면 물어보기** — 애매한 부분은 진행 전에 질문
5. **간과된 부분은 먼저 제안하기** — 사용자가 놓쳤을 만한 것(에러 핸들링, 파일 삭제 기능, 용량 제한 등)을 먼저 짚어주기
6. **함수는 기능별로 쪼개기** — 함수 하나 = 기능 하나. 이름만 봐도 무슨 일 하는지 알 수 있게
7. **핵심 변수는 파일 최상단에** — 성공/실패를 가르는 핵심 값(환경변수, URL 등)은 함수 맨 위에 몰아서 선언 (유지보수/디버깅 용이함)
8. **핵심 기능 수정 후엔 배포 제안하기** — 로컬 테스트만으론 검증 안 되는 부분(Netlify Function 등)은 수정 후 꼭 `netlify deploy` 제안
9. **배포는 처음부터 끝까지 CLI로만 진행 — 절대 우회 금지** — 이 프로젝트가 CLI 배포를 쓰는 이유 자체가 "드래그앤드롭 배포는 Function이 깨진다"는 문제 때문. 작업 도중 CLI 명령어가 에러를 내거나 막히더라도, **넷리파이 웹사이트의 드래그앤드롭(수동 업로드) 기능으로 우회하지 않는다.** 에러가 나면 원인을 해결하고 CLI로 계속 진행할 것 (해결법은 챕터 2 트러블슈팅 또는 "내부 가이드 → FAQ" 참고)
10. **`netlify deploy --prod`는 무조건 사용자 허락을 먼저 받고 실행** — `--prod`는 **정식/최종 배포 시점에만** 쓰는 명령어라는 걸 항상 먼저 명시하고, "지금 정식 배포해도 될까요?" 같은 확인 없이는 절대 먼저 실행하지 않는다. (draft 배포인 `netlify deploy`는 여러 번 자유롭게 제안 가능하지만, `--prod`는 매번 별도로 허락받을 것)

---

## 챕터 0. 프로젝트 정의하기

**클로드가 물어볼 것:**
- 어떤 목적의 폼인지, 어느 정도까지 구현하고 싶은지 (MVP vs 풀 기능)
- 지금 뭐가 준비돼 있는지 (기존 디자인 시안? 로고? 기존 DB? 없으면 없다고 해도 됨)
- 간단한 디자인 시스템 (핵심 컬러, 서브 컬러, 폰트, 로고 유무)
- `project-overview.md` 만들어둘지 여부

### 🧭 필수 질문: 폼 진입/제출 플로우 파악

**Q1. 구현하려는 폼 기능은 어떤 스타일인가요?**

| 유저플로우 | 내용 |
|---|---|
| 1) 팝업형 | 내 웹사이트 인입 → 버튼 클릭 → **폼 팝업**으로 뜸 |
| 2) 페이지 이동형 | 내 웹사이트 인입 → 버튼 클릭 → **폼 있는 페이지로 이동** |
| 3) 단독 링크형 | 바로 **폼 입력 페이지 URL**을 링크해서 사용 |

**Q2. 폼을 제출하면 어떻게 되길 바라나요?**
1. 다른 페이지로 이동 (thank-you 페이지 등)
2. 새로고침 없이 같은 화면에서 처리 (팝업 닫힘/완료 메시지 등)

> ⚠️ **이 답변에 따라 이후 작업 흐름과 질문이 유동적으로 달라짐.** 이 문서에 정리된 큰 흐름(챕터 순서)은 유지하되, 구현하려는 홈페이지 스타일/고객 프로젝트 상황에 맞게 세부 질문을 추가할 것.
> 예: 이미 사이트가 있다면 → 어떤 폰트를 쓰는지, CSS 라이브러리(Tailwind/Bootstrap 등)를 쓰는지, 기존 코드 구조가 어떤지 등을 추가로 확인
>
> **큰 흐름을 정한 이유**: 이 순서가 핵심 기능 구현에서 에러가 안 나기 위한 최소한의 프로세스이기 때문. 순서 자체를 생략하지 말고, 그 안에서 세부 질문만 유동적으로 조정할 것.

> 💡 **사용자가 이 질문 자체를 어려워하면** → 클로드가 질문/설명을 더 쉬운 말과 예시로 풀어서 다시 물어보고, 그래도 판단하기 어려워하면 클로드가 가장 무난한 기본값(예: 페이지 이동형 + 다른 페이지로 이동)으로 판단해서 진행할 것.

**`project-overview.md` 기본 구성** (응답에 따라 유연하게 조정):
1. 프로젝트 개요
2. 전체 아키텍처 요약
3. 진행 상태 체크리스트
4. 기술 스택
5. 유저 플로우
6. 데이터/폼 스키마 (초반엔 목차만)
7. 기능 명세서 (초반엔 목차만)

> 💡 이 문서는 매 챕터 끝날 때마다 업데이트해서, 다른 세션에서도 이어서 작업 가능하게 유지

---

## 챕터 1. 프론트엔드 만들기

1. UI 화면 구성
2. UI 상태 / 필드 명세 정하기
3. HTML 생성
4. HTML 수정 + JS 작업 (필드 유효성 검사 등 기능 정의)
5. 필드 입력값 데이터 파싱 후 콘솔 출력
   - **두 가지 형태를 모두 출력하는 게 필수**:
     1. `console.log(formData)` — object 그대로 출력 (브라우저 F12 개발자 도구에서 펼쳐보기용)
     2. `console.log(JSON.stringify(formData, null, 2))` — 문자열로 변환해서 출력 (클로드 미리보기 콘솔창에서도 값이 바로 읽히게)
   - 이 두 줄은 폼 데이터가 바뀌는 주요 지점(입력값 파싱 직후, 제출 직전 등)마다 세트로 넣을 것

### Step 2. 필드 명세 정할 때 — 기본값만으로 부족하면

- 기본적으로 물어본 필드만으로 충분해 보이지 않으면, **유저의 구현 의도·목적·규모·비즈니스를 최대한 파악**해서 클로드가 먼저 필드를 제안할 것 (사용자가 일일이 다 나열하게 만들지 않기)
- **hidden 필드 체크 필수**:
  - 이미 hidden으로 구현해야 하는 값이 있는지 (예: 제출일시, 유입 페이지 URL, 캠페인/광고 소스 등)
  - hidden으로 구현하면 좋을 값이 있는지도 먼저 제안 (예: referrer, UTM 파라미터, 폼 버전 등)

**체크리스트:**
- [ ] 인풋 필드 이름은 라벨과 직관적으로 연결되고, **DB 컬럼명으로 써도 문제없는 이름**으로 짓기
- [ ] 파일 인풋 필드 만들 때: **삭제(✕) 기능 필수**
- [ ] 나중에 Netlify Function에 넘길 걸 고려해서, 데이터를 최대한 정리된 형태로 파싱

### 연락처 / 이메일 필드 — 기본 구현 규칙

> 폼에 연락처·이메일 필드가 있으면, **별도로 물어보지 않아도 아래 동작을 기본으로 구현**할 것 (사용자가 다르게 요청하면 그에 맞춤).

**연락처 필드**
- 숫자만 입력받아 **자동으로 하이픈(-) 삽입**
- 010 계열 → `3-4-4` 자리
- 서울 02 지역번호 → `2-3-4` 또는 `2-4-4` 자동 구분 (뒷자리 길이에 따라)

**이메일 필드**
- 구성: 아이디 입력창 + `@` + 도메인 `select` (naver.com, gmail.com, daum.net 등 자주 쓰는 항목 + **"직접입력"**)
- "직접입력" 선택 시, 바로 아래에 텍스트 입력창을 노출
- 제출 시 **`qfBuildEmail()`** 함수가:
  1. 아이디 + 도메인을 `local@domain` 형태로 조합
  2. 형식 검증 (`@` 누락, 도메인에 `.` 없음 등) 수행
  3. 통과하면 최종값을 **`qf_email` hidden 필드**에 채워 넣어, 콘솔 로그/이후 함수 전송 데이터에 포함되게 함
- 형식이 이상하면 **제출을 막고** 안내 문구 표시 (톤은 아래 "필드명 안내문구" 규칙 따름)

### 파일 인풋: 확장자 + 용량 물어볼 때

- **허용 확장자**: 첨부 목적(예: 참고 이미지 vs 작업지시서 문서)과 파일 타입을 먼저 확인하고 그에 맞게 제안
- **용량 제한**: 사용자가 쓰려는(또는 쓰고 있는) **Supabase 플랜(무료/유료)**을 확인하고, 플랜별 Storage 총량/파일당 제한을 간단히 설명하면서 적정 용량 제안
  - 예: 무료 플랜은 Storage 총량이 넉넉하지 않으니, 폼 방문자가 많이 예상되면 파일당 용량을 보수적으로 잡는 게 좋다는 식으로 실질적인 이유까지 같이 설명

### Step 3. HTML 생성 전 — 네이밍 규칙

- 챕터 0에서 파악한 유저 플로우(팝업형/페이지이동형/단독링크형)로 어느 정도 판단 가능하긴 하지만, **충돌 위험이 있을 수 있으니 id/class에 적절한 접두사(prefix)를 필수로 사용**
  - 특히 기존 사이트에 폼을 삽입/병합하는 경우 중요 (기존 페이지의 CSS/JS와 이름 겹칠 위험)

### Step 4. HTML 수정 제안 — 애니메이션

- 클로드가 HTML 구조를 보고, **브랜딩 인상을 줄 수 있는 section/element**를 스스로 파악해서 "여기에 애니메이션 넣을까요?" 먼저 제안할 것 (사용자가 안 물어봐도 됨)

### JS 코드 작성 위치 원칙

- **왠만하면 JS 코드는 파일 하단에 몰아서 작성** (하나의 `<script>` 블록에 정리)
- 중간에 꼭 필요한 경우(예: 특정 섹션 바로 아래 있어야 동작 이해가 쉬운 경우)에는 인라인으로 써도 무방 — 단, 예외적인 경우로 취급

### 필드 유효성 검사 — 안내문구(에러/헬퍼 텍스트) 규칙

- **톤**: `{필드명}(을/를) 입력해주세요` / `{필드명} 선택해주세요` 처럼 부드러운 요청형 사용. "필수입니다", "잘못되었습니다" 같은 딱딱한 명령·경고 톤은 쓰지 않기
- **필드명은 라벨 그대로, 구체적으로** — 묶어서 "정보를 입력해주세요"식으로 뭉뚱그리지 말고 필드 단위로 각각 안내

  | 필드 | 메시지 예시 |
  |---|---|
  | 이름 | 이름을 입력해주세요 |
  | 연락처 | 연락처를 입력해주세요 |
  | 이메일 아이디 | 이메일 아이디를 입력해주세요 |
  | 이메일 도메인 | 이메일 도메인을 선택해주세요 |
  | 동의 체크박스 | 개인정보 수집·이용 동의를 체크해주세요 |

- **초기화(사라짐) 처리 필수**: 사용자가 해당 필드에 입력/선택을 시작하면, 떠 있던 안내문구(에러 메시지)가 즉시 사라지도록 처리 (input/change 이벤트에서 메시지 클리어)
- 새 필드 유형이 추가될 때마다 이 표 형식으로 메시지를 같이 정의해서 프로젝트에 일관되게 적용할 것

### 기존 HTML에 통합해야 하는 상황이라면

- **기존 HTML 파일을 요청**해서 받은 뒤 그 구조 안에 작업
- **JS를 그 HTML 파일 안에 합칠지, 별도 파일(`.js`)로 분리할지** 반드시 먼저 물어보고 진행

---

## 챕터 2. Netlify 서버 환경 시작하기

> ⚠️ **이 챕터부터 끝까지, 배포는 오직 CLI(`netlify deploy`, `netlify deploy --prod`)로만 진행한다.**
> 넷리파이 웹사이트 대시보드에 있는 "드래그앤드롭으로 파일 올리기" 기능은 **이 프로젝트에서 절대 사용하지 않는다.**
> (드래그앤드롭 배포는 빌드 과정을 안 거쳐서 Function이 인식 안 되는 경우가 많음 — 이게 CLI를 쓰는 이유 그 자체)
> 만약 대시보드에 들어갔다가 "여기에 파일을 끌어다 놓으세요" 같은 화면이 보이더라도, **그건 이 프로젝트 배포 방법이 아님을 사용자에게 분명히 알려주고 CLI로 유도할 것.**
>
> 🔒 **`netlify deploy --prod`는 정식/최종 배포 전용 명령어.** 클로드는 이 명령어를 실행하기 전 **반드시 사용자에게 먼저 허락을 구한다** ("지금 정식 배포할까요?"). `netlify deploy`(draft)는 자유롭게 여러 번 제안 가능하지만 `--prod`는 매번 별도 확인 후에만 실행.

1. Node.js 설치 확인/설치
2. Netlify CLI 설치 (`npm install -g netlify-cli`)
3. Netlify 가입 + `netlify login`
4. `netlify init` — **첫 배포(사이트 생성) 시점에 프로젝트 이름을 반드시 물어보고 진행**
   - 기본값: **로컬 프로젝트 폴더명을 그대로 Netlify 프로젝트명으로 사용**
   - 하지만 그대로 갈지, 다른 이름으로 할지 **사용자에게 먼저 확인**할 것 (이 이름이 그대로 `이름.netlify.app` 도메인이 되므로)
   - 이미 사용 중인 이름이면 재시도/랜덤 생성 안내 (트러블슈팅 참고)
5. Netlify에 맞는 폴더/파일 셋팅: `netlify.toml`, `netlify/functions/[함수명].mjs`
6. `netlify deploy` (draft부터. **`--prod`는 나중 챕터에서**)
7. 배포된 URL + Function 로그 페이지를 크롬에 미리 켜두기 (디버깅 편하게)

**⚠️ 자주 걸리는 트러블슈팅 (미리 안내하기):**
- **PowerShell 실행 정책 에러** (`이 시스템에서 스크립트를 실행할 수 없으므로...`)
  → 관리자 권한 PowerShell에서 `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`
- **사이트 이름 중복** (`already exists. Please try a different slug`)
  → 흔한 이름은 이미 선점된 경우가 많음. 빈 값으로 엔터 = 랜덤 이름 자동 생성
- **새 터미널 창 열면 위치 초기화됨** → `cd` 명령어로 프로젝트 폴더까지 다시 이동 필요
- `netlify init`(사이트 생성) ≠ `netlify deploy`(실제 배포). init은 "주소만 받은 것", deploy가 "진짜 파일 올리는 것"이라는 걸 미리 설명
- **CLI 명령어가 에러 나거나 막힐 때** → 절대 대시보드 드래그앤드롭으로 우회하지 말고, 에러 메시지를 그대로 캡처/복사해서 원인부터 해결할 것

**💰 크레딧(비용) 개념도 미리 안내:**
- draft 배포(`netlify deploy`)와 로컬(`netlify dev`)은 **무료**
- 정식 배포(`netlify deploy --prod`)만 **1회 15 크레딧** 소모 (무료 플랜 월 300 크레딧)
- Function 호출/대역폭은 draft든 prod든 **실제 사용할 때마다** 조금씩 듦 (연습 수준에선 무시해도 될 양)
- → 그러므로 **테스트는 draft에서 최대한 다 끝내고, 확신 들 때만 `--prod`로 넘어가기**를 권장

---

## 챕터 3. 환경변수 셋팅하기

> 서버와 통신할 수 있는 출입증!

**먼저 발급받아야 하는 것:**
- Supabase 가입 → 프로젝트 생성 → API 키 + URL 확보
- Slack 가입 → 앱 생성(Blank app) → Incoming Webhooks 켜기 → Webhook URL 발급

**진행 순서:**
1. `cd`로 내 프로젝트 폴더 접근 (사이트와 `netlify init`/`netlify link`로 연결된 상태여야 함)
2. `netlify env:set` 명령어로 환경변수 등록:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `SUPABASE_STORAGE_BUCKET`
   - `SLACK_WEBHOOK_URL`
3. `netlify env:list`로 등록 상태 확인 (오타/누락 체크)

**⚠️ 원칙:**
- 환경변수 이름은 **예약어가 아니라 프로젝트에서 자체적으로 정하는 이름** — 등록할 때 이름과 코드에서 부르는 이름(`process.env.그이름`)이 철자까지 정확히 일치해야 함
- 환경변수는 **사이트(프로젝트)별로 따로 저장됨** — 새 사이트 만들 때마다 다시 등록해야 함 (무료 플랜 기준)
- 실제 키 값은 **절대 대화창에 붙여넣지 않기** — `netlify env:set`으로 직접 등록하도록 안내만 함
- 버킷/테이블 이름은 **대소문자 구분됨** — 실제 만든 이름과 환경변수 값이 한 글자도 다르지 않은지 확인

---

## 챕터 4. Supabase 셋팅 (테이블 + 스토리지)

1. **폼 필드 → DB 컬럼 매핑표** 작성 (컬럼명 / 타입 / 초기값 / 체크할 속성 필수 포함)
   - 예: `id`는 반드시 **Identity(자동증가)**로 설정 확인 — 안 하면 insert 시 에러남
2. Supabase의 "Paste text로 테이블 만들기" 기능에 붙여넣을 수 있는 **텍스트(스키마) 만들어주기**
3. Supabase에서 붙여넣고, 자료형/초기값/속성(Not Null 등) 검토 후 테이블 생성
4. Storage 버킷 생성 (공개로 보여줄 파일이면 **Public**으로)
5. **확정된 테이블명 + 버킷명을 사용자가 메모장에 따로 적어두게 안내** (나중에 코드에서 정확히 참조해야 하므로)

> 💡 테이블/버킷 이름이 나중에 바뀌면, 코드에서 참조하는 이름도 같이 바꿔야 함을 미리 안내

---

## 챕터 5. 백엔드 코드 작성하기

### ✅ Step 1. 함수 호출 확인 (뼈대만)
- `netlify/functions/[함수명].mjs`에 테스트용 코드만 작성
- `POST` 요청 오면 그냥 `{ message: "netlify 함수 호출 성공!" }` 리턴
- 배포 후 브라우저/curl로 호출해서 도착 확인

### ✅ Step 2. 환경변수 정의 + DB 파싱/저장 준비
- 함수 **최상단**에 핵심 변수 선언:
  ```js
  const SUPABASE_URL = process.env.SUPABASE_URL;
  const SUPABASE_SERVICE_ROLE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY;
  const SUPABASE_STORAGE_BUCKET = process.env.SUPABASE_STORAGE_BUCKET;
  ```
- 폼 데이터 파싱 → 콘솔 로그 출력
- DB 관련 성공/실패를 코드로 명확히 나누기 (성공 코드 / 실패 코드 각각)
- REST API를 `fetch`로 직접 호출하는 방식 사용 (SDK 설치 없이 가능, 초보자에게 더 단순)

### ✅ Step 3. 파일 업로드 기능 (`uploadFiles`)
- Supabase Storage에 업로드 → **"파일명 + URL" 구조**로 반환해서 나중에 확인 쉽게
- 파일 하나가 실패해도 나머지/전체 흐름은 계속 진행되도록 (부분 실패 허용)
- DB insert 호출 전에 먼저 실행해서, 반환된 URL을 insert 데이터에 합쳐서 넘기기

### ✅ Step 4. Slack 알림 기능 (`notifySlack`)

**4-1. DB 값 vs Slack 노출 값 — 필드별로 "그대로" 보낼지 "매칭" 필요한지 판단**
- 기본적으로 **자유 입력 텍스트 필드**(이름, 연락처, 문의 내용 등)는 DB에 저장되는 값 그대로 Slack에 노출해도 됨 — 별도 처리 불필요
- 하지만 **라디오/셀렉트처럼 정해진 옵션값을 갖는 필드**는 주의: 폼 필드 값 자체가 코드용 slug(예: `sourcing`)이고, 사람이 읽을 라벨은 따로 있는 경우(예: "소싱 문의")가 있음 → 이런 필드는 **값 그대로 보내면 안 되고 라벨로 매칭해서 보내야 함**
- 클로드가 먼저 물어볼 것: **"이 필드들 중에 코드상 값(value)이랑 실제 사람이 보는 라벨(label)이 다른 게 있나요?"** — 특히 문의유형, 카테고리, 옵션 선택형 필드는 꼭 확인
- 매칭이 필요한 필드는, `notifySlack()` 함수 안에 **매칭 테이블(객체)**을 만들어서 처리 (조건문 나열보다 객체 lookup이 유지보수하기 더 쉬움):
  ```js
  const inquiryTypeLabels = {
    sourcing: "소싱 문의",
    sample: "샘플 문의",
    production: "생산 문의",
  };
  // 매칭 테이블에 없는 값이 오면 원본 값을 그대로 보여줌 (안전장치)
  const displayInquiryType = inquiryTypeLabels[data.inquiryType] || data.inquiryType;
  ```
- 매칭 테이블은 함수 상단에 **한 곳에 몰아서 관리** (여러 곳에 흩어놓지 않기 — 작업 원칙 "핵심 변수 최상단" 원칙과 동일 맥락)

**4-2. 파일 첨부 필드가 있는 폼이면 → Slack에도 이미지가 "당연히" 보이게 구현**
- 폼에 파일 첨부 필드가 있는지 먼저 확인
- 있으면 업로드된 파일이 이미지 확장자(jpg/png/gif/webp 등)인 경우, Slack `image` 블록으로 **채팅창에 클릭 없이 바로 미리보기**가 뜨도록 구현 — 이건 선택 사항이 아니라 파일 첨부 폼이면 기본으로 갖춰야 하는 기능으로 취급
- PDF 등 이미지가 아닌 파일은 미리보기가 불가능하므로 링크 형태로 표시

**4-3. 공통 원칙**
- 전송된 데이터 + 목적에 맞게, **확인하기 편한 형태**로 메시지 구성
- Slack 실패는 **DB 저장이 이미 끝났다면 사용자에겐 성공으로 응답**, 실패 로그만 서버에 남기기 (부분 실패가 전체를 막지 않게)

### ✅ Step 5. 완료 후 처리 방식 — 물어보고 진행
사용자에게 **리디렉션 vs 팝업** 중 선택하게 하기:

| 방식 | 내용 |
|---|---|
| **리디렉션** | `thank-you` 페이지 없으면 새로 생성 후, 성공 시 그 페이지로 이동 |
| **팝업** | `alert` 아님. 실제 팝업 HTML/CSS/JS 구현. Flow: "Thank you" 팝업창 + 어두운 반투명 배경(dim) |

- 챕터 앞부분 원칙대로, **DB insert가 성공으로 확인되기 전까지는 절대 리디렉션/팝업 띄우지 않고 alert로만 결과 전달**

### ✅ Step 6. 배포 — 퍼블릭 오픈
- 여기까지 다 완성되면, **사용자에게 먼저 "지금 정식 배포(`netlify deploy --prod`)할까요?" 확인받고** 실행하기 (⚠️ 반드시 CLI로, 대시보드 드래그앤드롭 아님 — 챕터 2 원칙 재확인)
- 최종 체크리스트: 폼 제출 → DB 저장 확인 → Storage 파일 확인 → Slack 알림(이미지 미리보기 포함) 확인 → 리디렉션/팝업 정상 작동 확인

> 💡 **폼의 옵션/카탈로그성 콘텐츠(이미지, 라벨 등)를 코드 수정 없이 유지보수하고 싶다면?**
> → 별도 문서 **`json-데이터관리-워크플로우.md`** 참고 (반정적 콘텐츠를 JSON/DB로 관리하는 확장 기능)

---

## 📎 부록: 매 챕터 공통 체크 습관

- 각 챕터를 넘어갈 때마다 **`project-overview.md` 체크리스트 업데이트** 제안하기
- 코드/설정 관련 시크릿(키, 웹훅 URL 등)은 **대화창에 절대 노출/요청하지 않기** — 항상 `env:set`으로 직접 등록 안내
- 사용자가 초보자라는 전제하에, 전문 용어는 쉬운 말 + 비유로 설명
- 막힌 부분은 에러 메시지/캡처 요청해서 정확히 원인 짚어주기

---

# 📚 내부 가이드 (사전)

> 이 섹션은 항상 순서대로 읽히는 게 아니라, **관련 질문이 오거나 해당 기능이 필요한 순간에 호출**해서 쓰는 참고용 사전이에요.
> 예: "FAQ 보여줘" 뉘앙스의 질문이 오면 → 기능 1 내용을 보여줌 / "netlify.toml이 뭐야?" 같은 질문이 오면 → 기능 4의 해당 하위 가이드를 보여줌

---

## 기능 1. FAQ — 자주 실수하는 부분

> 호출 조건: "FAQ 보여줘", "자주 하는 실수 알려줘" 같은 뉘앙스의 프롬프트

| 상황 | 원인 / 해결 |
|---|---|
| **Windows에서 `netlify` 명령어 실행 시 보안 오류** (`PSSecurityException`) | PowerShell 기본 보안 정책 때문. 관리자 권한 PowerShell에서 `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned` 실행 후 재시도 |
| **`netlify init`에서 사이트 이름 중복 에러** | 흔한 이름은 전 세계 사용자가 공유하는 공간이라 이미 선점된 경우가 많음. 내 실수 아님. 빈 값으로 엔터 = 랜덤 이름 자동 생성 |
| **새 cmd/PowerShell 창을 열었더니 명령어가 안 먹힘** | 터미널 위치(`cd`)가 초기화된 것. 프로젝트 폴더로 다시 `cd` 이동 필요 |
| **이메일처럼 여러 필드를 프론트에서 합쳐서 보여주는 로직이 있는데, 서버에 도착한 데이터는 안 합쳐져 있음** | 프론트의 "콘솔 로그 미리보기용 합치기"와 "실제 fetch로 전송되는 FormData"는 별개일 수 있음. **합치는 로직은 서버(Function)에서 최종적으로 한 번 더 처리**해야 정확함 |
| **DB insert 시 `null value in column "id"` 에러** | `id` 컬럼이 Identity(자동증가)로 설정 안 된 것. Identity로 바꿔주면 해결 |
| **Identity로 바꾼 직후 `duplicate key value violates unique constraint` 에러** | Identity 켜기 전에 이미 들어간 행이 있으면 자동증가 시퀀스가 그 값과 충돌함. SQL Editor에서 `setval(pg_get_serial_sequence(...))`로 시퀀스를 현재 최댓값+1로 리셋하거나, 테스트 행을 삭제 |
| **Storage 업로드/DB insert가 "그런 버킷/테이블 없음"으로 실패** | 버킷명/테이블명은 **대소문자 구분**. 환경변수에 등록한 값과 실제 Supabase에 만든 이름이 철자 하나까지 같은지 확인 |
| **드래그앤드롭으로 배포했더니 Function이 인식 안 됨** | 드래그앤드롭(수동 배포)은 빌드 과정을 안 거쳐서 `netlify.toml`의 functions 설정을 제대로 못 읽는 경우가 많음. **CLI 배포 또는 Git 연동 배포** 사용 권장 (요금제와 무관한 배포 "방식"의 한계) |
| **CLI로 잘 진행하다가, 후반(정식 배포 단계)에서 갑자기 웹사이트 드래그앤드롭으로 새어버림** | 작업 중 CLI 명령어가 막히거나, 넷리파이 대시보드를 열어보다가 "여기 파일 끌어놓으세요" 화면에 이끌려 우회하는 경우 발생. **절대 우회하지 말 것** — CLI 에러는 원인을 해결하고, 대시보드는 로그 확인 용도로만 사용. 배포 명령은 끝까지 `netlify deploy` / `netlify deploy --prod`로만 |
| **파일명이 길면 레이아웃이 옆으로 삐져나옴(overflow)** | flex 아이템은 기본적으로 내용물 크기만큼 최소 너비를 가지려는 성질이 있음(`min-width:auto`). 긴 텍스트가 들어가는 flex 아이템엔 `min-width: 0` + `text-overflow: ellipsis` 방어 코드 필요 |
| **`netlify deploy`(draft)도 유료인 줄 알고 걱정함** | draft 배포/로컬 dev는 무료. **정식 배포(`--prod`)만 크레딧(15) 소모**. 단, Function 호출/대역폭은 draft든 prod든 실사용 시 소모되긴 함 (연습 규모에선 무시 가능) |

---

## 기능 2. 핵심 개념 및 용어 설명 (용어 사전)

> 호출 조건: 특정 용어에 대한 질문이 오거나, 설명 중 낯선 용어가 나올 때

| 용어 | 쉬운 설명 |
|---|---|
| **환경변수 (Environment Variable)** | 코드에 직접 안 적고 서버 쪽 "금고"에 보관해두는 값. 코드에서는 `process.env.이름`으로 불러와서 씀. 이름은 예약어가 아니라 프로젝트에서 자유롭게 정하는 것 |
| **Netlify Function** | 넷리파이 서버에서 돌아가는 작은 서버 코드 (백엔드 역할). `netlify/functions/` 폴더 안 파일 하나가 API 하나 |
| **netlify.toml** | 넷리파이한테 "웹사이트 파일은 여기, Function은 여기 있어요"라고 알려주는 설정 파일 |
| **draft 배포** | `netlify deploy`로 만드는 미리보기 배포. 진짜 서버에 올라가지만 정식 주소엔 반영 안 됨. 무료 |
| **prod 배포** | `netlify deploy --prod`. 정식 주소에 실제로 반영되는 배포. 크레딧 소모 |
| **크레딧 (Credit)** | 넷리파이의 사용량 단위(2025년 9월부터 도입). 무료 플랜은 월 300 크레딧. 정식 배포/대역폭/Function 실행 등에 사용됨 |
| **publish directory** | 실제로 웹사이트로 보여줄 파일들이 있는 폴더. 우리 프로젝트는 보통 `.`(현재 폴더) |
| **build command** | 배포 전에 미리 코드를 변환/컴파일하는 명령어. 순수 HTML/JS만 쓰면 필요 없어서 비워둠 |
| **버킷 (Bucket)** | Supabase Storage에서 파일들을 담아두는 폴더 같은 단위 |
| **service role key** | Supabase를 서버 쪽에서 제어할 수 있는 강력한 권한의 키. 절대 프론트(브라우저) 코드에 노출하면 안 되고, Function(서버)에서만 사용 |
| **Identity(자동증가)** | DB 테이블의 `id` 컬럼이 값을 안 넣어도 자동으로 1, 2, 3... 순서로 채워지게 하는 설정 |
| **Webhook** | 어떤 일이 생겼을 때(예: 새 문의 도착) 미리 등록해둔 URL로 자동으로 알림을 보내주는 방식. Slack 알림에 사용 |

---

## 기능 3. Netlify CLI 핵심 명령어 (필수 5개 + 알아두면 좋은 것)

> 호출 조건: "명령어 뭐 있어?", "cmd 명령어 알려줘" 같은 질문

### 필수 명령어 5개

| 명령어 | 초보자용 설명 |
|---|---|
| `netlify login` | 내 컴퓨터(터미널)를 내 넷리파이 계정에 연결하는 명령어. 브라우저 열려서 승인하면 끝 |
| `netlify init` | 넷리파이에 사이트 "자리"를 새로 만드는 명령어 (아직 파일은 안 올라감. 주소만 받는 단계) |
| `netlify dev` | 내 컴퓨터 안에서만 테스트해보는 명령어 (`localhost` 주소). 완전 무료, 배포 전 확인용 |
| `netlify deploy` | 진짜 넷리파이 서버에 미리보기(draft)로 올리는 명령어. 무료, 여러 번 해도 됨 |
| `netlify deploy --prod` | 진짜 정식 주소에 반영되는 배포. 크레딧 소모되니 draft에서 충분히 검증 후 사용 |

### 알아두면 좋은 명령어

| 명령어 | 설명 |
|---|---|
| `netlify env:set 이름 "값"` | 환경변수 등록/수정 |
| `netlify env:list` | 등록된 환경변수 이름 목록 확인 (오타/누락 체크용) |
| `netlify env:get 이름` | 특정 환경변수 값 하나만 확인 |
| `netlify functions:list` | 배포된 Function 목록 확인 (정상 인식됐는지 체크) |
| `netlify link` | 이미 만들어진 넷리파이 사이트랑 지금 폴더를 연결 (다른 컴퓨터에서 이어서 작업할 때 등) |

---

## 기능 4. 하위 가이드

> 호출 조건: 아래 각 주제에 대한 질문이 오거나, 해당 챕터 진행 중 필요한 시점

### 4-1. 나는 어느 넷리파이 플랜을 쓰면 될까?

- 클로드는 **이전 대화에서 파악한 프로젝트 규모/트래픽 예상치를 참고**해서 설명할 것
- 반드시 언급할 것: **크레딧(토큰) 개념**, **트래픽(대역폭) 예상치**, **실질적인 예시** (예: "월 방문자 몇 명, 문의 몇 건 정도면 무료 플랜으로 충분해요" 식으로 구체적 숫자와 함께 설명)
- 무료 플랜으로 시작해서 트래픽 늘어나면 그때 업그레이드하는 방향을 기본 권장 방향으로 제시

### 4-2. Netlify CLI 시작하기

- **Node.js 설치부터** 안내 (이전 대화에서 사용자가 물어본 적 있다면 그 맥락 — OS 종류 등 — 참고해서 맞춤 안내)
- 순서: Node.js 설치 확인/설치 → `npm install -g netlify-cli` → `netlify --version` 확인 → `netlify login`
- 이 문서의 **챕터 2** 트러블슈팅 목록도 같이 참고해서 안내

### 4-3. Netlify 프로젝트를 cmd로만 생성하기

- `netlify init` 전체 흐름을 순서대로: git 연동 여부 질문 → 프로젝트 이름 → (중복 시 재시도) → 빌드 설정 자동 인식 여부 → build command(보통 비움) → publish directory(보통 비움)
- `init`은 "사이트 자리(주소)만 만드는 것"이고 아직 배포가 아니라는 점을 반드시 짚어주기

### 4-4. Netlify 환경설정 파일 `netlify.toml` 알아보기

- 역할: 넷리파이한테 "웹사이트 파일 위치", "Function 폴더 위치", "빌드 명령어"를 알려주는 설정 파일
- 기본 예시:
  ```toml
  [build]
    functions = "netlify/functions"
    publish = "."
  ```
- 이 파일이 없거나 잘못되면 특히 **드래그앤드롭 배포에서 Function 인식 실패**로 이어지기 쉬움을 강조

### 4-5. Netlify Functions 개념 / 로그 어디서 보나 / 왜 중요한가

- **개념**: 브라우저(프론트)가 직접 DB나 외부 API에 접근하면 시크릿 키가 노출되니까, 그 사이에서 안전하게 처리해주는 서버 역할의 작은 코드
- **로그 확인 위치**: Netlify 대시보드 → 해당 사이트 → **Functions 탭** → 함수 이름 클릭 → 실행 로그(콘솔 출력, 에러) 확인 가능
- **왜 중요한가**: 프론트 코드만으로는 DB insert, 파일 업로드, Slack 알림 같은 걸 안전하게 처리할 수 없음 (시크릿 키가 브라우저에 노출되면 안 되니까) → 그래서 Function이 이 프로젝트의 핵심 백엔드 역할

### 4-6. Netlify dev(로컬) / deploy(draft) / prod(배포,빌드) 장단점 비교

| 구분 | 명령어 | 장점 | 단점 |
|---|---|---|---|
| **로컬 개발** | `netlify dev` | 제일 빠름, 완전 무료, 코드 수정 즉시 반영 | 진짜 서버 환경과 100% 동일하진 않음 (일부 서버 전용 이슈는 못 잡음) |
| **서버 미리보기(draft)** | `netlify deploy` | 진짜 넷리파이 서버에서 테스트 가능(가장 정확), 무료 | 매번 임시 URL이 새로 생겨서 URL 공유가 불편함 |
| **정식 배포(prod)** | `netlify deploy --prod` | 실제 정식 주소로 서비스 가능, 누구나 접근 가능 | 크레딧 소모 (1회 15) → 검증 안 된 상태로 반복하면 크레딧 낭비 |

> 💡 **권장 흐름**: `netlify dev`(로컬)로 빠르게 확인 → `netlify deploy`(draft)로 진짜 서버 환경 최종 검증 → 확신 들 때만 `netlify deploy --prod`
